:plain
  ---
  layout: default
  title: Alex's Tech Blog
  ---
%h1 Тефтер
{% for post in site.posts %}
.post
  %h2
    %a{ :href => "{{ post.url }}" } {{ post.title }}
  %p {{ post.content | strip_newlines | strip_html | truncatewords: 60 }}
  %span.date {{ post.date | date_to_string }}
  %a.readmore{ :href => "{{ post.url }}" } Readmore →
{% endfor %}
